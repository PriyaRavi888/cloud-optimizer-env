[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "cloud-optimizer-env"
version = "0.1.0"
description = "Cloud optimizer environment"
requires-python = ">=3.9"

dependencies = []